import fs from 'node:fs';

const htmlPath='detektivnaya-igra-s-ii/index.html';
const jsPath='assets/ai-detective-vslice.js';
const cssPath='assets/ai-detective-vslice.css';
const testPath='tests/ai-detective-vslice-contract.mjs';

function replaceOnce(text,from,to,label){
  if(!text.includes(from)) throw new Error(`missing ${label}`);
  if(text.indexOf(from)!==text.lastIndexOf(from)) throw new Error(`duplicate ${label}`);
  return text.replace(from,to);
}

let html=fs.readFileSync(htmlPath,'utf8');
html=replaceOnce(html,'../assets/ai-detective-vslice.css?v=0.1.0','../assets/ai-detective-vslice.css?v=0.1.1','CSS cache key');
html=replaceOnce(html,
`        <div class="aid-transcript" data-transcript aria-live="polite"></div>\n\n        <form class="aid-composer" data-composer>`,
`        <div class="aid-transcript" data-transcript aria-live="polite"></div>\n\n        <div class="aid-case-progress" data-case-progress hidden>\n          <div>\n            <p class="aid-kicker">Признание получено</p>\n            <strong>Допрос завершён. Теперь оформите обвинение.</strong>\n            <span>Назовите виновного и своими словами свяжите доступ, местонахождение, окно камеры и алиби остальных.</span>\n          </div>\n          <button class="aid-primary" type="button" data-action="theory-inline">Оформить обвинение</button>\n        </div>\n\n        <form class="aid-composer" data-composer>`,
'confession transition');
html=replaceOnce(html,
`        <div class="aid-theory-box">\n          <p class="aid-kicker">Когда будете готовы</p>\n          <button class="aid-secondary" type="button" data-action="theory">Собрать версию</button>\n        </div>`,
`        <div class="aid-theory-box" data-theory-box>\n          <p class="aid-kicker" data-theory-kicker>Когда будете готовы</p>\n          <p class="aid-theory-hint" data-theory-hint>Когда доказательств достаточно, сформулируйте собственную версию дела.</p>\n          <button class="aid-secondary" type="button" data-action="theory">Собрать версию</button>\n        </div>`,
'theory CTA');
html=replaceOnce(html,'../assets/ai-detective-vslice.js?v=0.3.3','../assets/ai-detective-vslice.js?v=0.3.4','JS cache key');
fs.writeFileSync(htmlPath,html);

let js=fs.readFileSync(jsPath,'utf8');
js=replaceOnce(js,
`const state={view:saved?.view||'intro',suspect:suspects.some(s=>s.id===saved?.suspect)?saved.suspect:'marina',attached:null,turns:Number.isInteger(saved?.turns)?Math.max(0,Math.min(saved.turns,MAX_TURNS)):0,transcripts:saved?.transcripts&&typeof saved.transcripts==='object'?saved.transcripts:initialTranscript(),notes:new Map(Array.isArray(saved?.notes)?saved.notes:[]),evidenceIds:new Set(Array.isArray(saved?.evidenceIds)?saved.evidenceIds:INITIAL_EVIDENCE),questionCounts:{marina:0,anton:0,lev:0,...(saved?.questionCounts||{})},busy:false,session:getSession(),visitor:getVisitor()};`,
`const state={view:saved?.view||'intro',suspect:suspects.some(s=>s.id===saved?.suspect)?saved.suspect:'marina',attached:null,turns:Number.isInteger(saved?.turns)?Math.max(0,Math.min(saved.turns,MAX_TURNS)):0,transcripts:saved?.transcripts&&typeof saved.transcripts==='object'?saved.transcripts:initialTranscript(),notes:new Map(Array.isArray(saved?.notes)?saved.notes:[]),evidenceIds:new Set(Array.isArray(saved?.evidenceIds)?saved.evidenceIds:INITIAL_EVIDENCE),questionCounts:{marina:0,anton:0,lev:0,...(saved?.questionCounts||{})},verdict:saved?.verdict&&typeof saved.verdict==='object'?saved.verdict:null,busy:false,session:getSession(),visitor:getVisitor()};`,
'verdict state');
js=replaceOnce(js,
`function saveState(){try{sessionStorage.setItem(STORAGE_KEY,JSON.stringify({view:state.view,suspect:state.suspect,turns:state.turns,transcripts:state.transcripts,notes:[...state.notes.entries()],evidenceIds:[...state.evidenceIds],questionCounts:state.questionCounts}))}catch{}}`,
`function saveState(){try{sessionStorage.setItem(STORAGE_KEY,JSON.stringify({view:state.view,suspect:state.suspect,turns:state.turns,transcripts:state.transcripts,notes:[...state.notes.entries()],evidenceIds:[...state.evidenceIds],questionCounts:state.questionCounts,verdict:state.verdict}))}catch{}}`,
'persist verdict');
js=replaceOnce(js,
`function renderRoom(){const s=suspects.find(x=>x.id===state.suspect);$('[data-suspect-name]').textContent=s.name;$('[data-suspect-role]').textContent=s.role;const tr=$('[data-transcript]');tr.innerHTML=state.transcripts[s.id].map(m=>\`<div class="aid-message \${m.who==='player'?'is-player':'is-suspect'}"><div class="aid-message-meta"><span>\${m.who==='player'?'Вы':escapeHtml(s.name)}</span><span>\${escapeHtml(m.meta||'')}</span></div><div>\${escapeHtml(m.text)}</div>\${m.evidence?\`<div class="aid-message-evidence">Материал: \${escapeHtml(m.evidence)}</div>\`:''}</div>\`).join('');tr.scrollTop=tr.scrollHeight;updateTurnCounter()}`,
`function renderRoom(){const s=suspects.find(x=>x.id===state.suspect);$('[data-suspect-name]').textContent=s.name;$('[data-suspect-role]').textContent=s.role;const tr=$('[data-transcript]');tr.innerHTML=state.transcripts[s.id].map(m=>\`<div class="aid-message \${m.who==='player'?'is-player':'is-suspect'}"><div class="aid-message-meta"><span>\${m.who==='player'?'Вы':escapeHtml(s.name)}</span><span>\${escapeHtml(m.meta||'')}</span></div><div>\${escapeHtml(m.text)}</div>\${m.evidence?\`<div class="aid-message-evidence">Материал: \${escapeHtml(m.evidence)}</div>\`:''}</div>\`).join('');tr.scrollTop=tr.scrollHeight;updateTurnCounter();renderCaseProgress()}`,
'render room finale');
js=replaceOnce(js,
`function addEvidence(id){const item=evidence.find(e=>e.id===id);if(!item||state.evidenceIds.has(id))return false;state.evidenceIds.add(id);addNote({id:\`SYS-\${id}\`,source:'Новый материал',text:\`Открыт материал «\${item.title}».\`});return true}\nfunction updateTurnCounter(){const el=$('[data-turn-counter]');const exhausted=state.turns>=MAX_TURNS;el.textContent=exhausted?\`\${MAX_TURNS} / \${MAX_TURNS} · соберите версию\`:\`\${state.turns} / \${MAX_TURNS} вопросов\`;const send=$('.aid-send');send.disabled=state.busy||exhausted;$('#aid-question').disabled=state.busy||exhausted}`,
`function addEvidence(id){const item=evidence.find(e=>e.id===id);if(!item||state.evidenceIds.has(id))return false;state.evidenceIds.add(id);addNote({id:\`SYS-\${id}\`,source:'Новый материал',text:\`Открыт материал «\${item.title}».\`});return true}\nfunction hasConfession(){return state.notes.has('N-MARINA-CONFESSION')}\nfunction renderCaseProgress(){const confessed=hasConfession();const progress=$('[data-case-progress]');if(progress)progress.hidden=!confessed;const composer=$('[data-composer]');if(composer)composer.classList.toggle('is-closed',confessed);const theoryBox=$('[data-theory-box]');if(theoryBox)theoryBox.classList.toggle('is-ready',confessed);const kicker=$('[data-theory-kicker]');if(kicker)kicker.textContent=confessed?'Признание получено':'Когда будете готовы';const hint=$('[data-theory-hint]');if(hint)hint.textContent=confessed?'Допрос завершён. Назовите виновного и соберите доказательную цепочку в обвинение.':'Когда доказательств достаточно, сформулируйте собственную версию дела.';const theoryBtn=$('[data-action="theory"]');if(theoryBtn)theoryBtn.textContent=confessed?'Оформить обвинение':'Собрать версию'}\nfunction updateTurnCounter(){const el=$('[data-turn-counter]');const exhausted=state.turns>=MAX_TURNS;const confessed=hasConfession();el.textContent=confessed?'Признание получено · оформите обвинение':exhausted?\`\${MAX_TURNS} / \${MAX_TURNS} · соберите версию\`:\`\${state.turns} / \${MAX_TURNS} вопросов\`;const send=$('.aid-send');send.disabled=state.busy||exhausted||confessed;$('#aid-question').disabled=state.busy||exhausted||confessed}`,
'case progress logic');
js=replaceOnce(js,
`function typing(on){state.busy=on;$('[data-room-status]').textContent=on?'Собеседник отвечает…':'Допрос идёт';updateTurnCounter();if(on){`,
`function typing(on){state.busy=on;$('[data-room-status]').textContent=on?'Собеседник отвечает…':hasConfession()?'Признание получено':'Допрос идёт';updateTurnCounter();if(on){`,
'truthful confessed status');
js=replaceOnce(js,
`function openTheory(){renderTheory();showView('theory')}\nfunction renderTheory(){const wrap=$('[data-theory-suspects]');wrap.innerHTML=suspects.map((s,i)=>\`<label class="aid-radio"><input type="radio" name="suspect" value="\${s.id}" \${i===0?'required':''}><span><strong>\${escapeHtml(s.name)}</strong> · \${escapeHtml(s.role)}</span></label>\`).join('')}\nasync function submitTheory(ev){ev.preventDefault();if(state.busy)return;const form=ev.currentTarget;const fd=new FormData(form);const picked=fd.get('suspect');const reason=fd.get('reason')?.toString().trim()||'';if(!picked||reason.length<8)return;const btn=form.querySelector('button[type="submit"]');btn.disabled=true;btn.textContent='Проверяем цепочку…';try{const data=await api({action:'check_theory',suspect_id:picked,reason,discovered_note_ids:[...state.notes.keys()].filter(id=>!id.startsWith('SYS-')),discovered_evidence_ids:[...state.evidenceIds],question_counts:state.questionCounts});const box=$('[data-verdict]');box.hidden=false;box.className=\`aid-verdict \${data.correct?'is-correct':'is-wrong'}\`;box.innerHTML=\`<p class="aid-kicker">\${data.correct?'Версия выдерживает проверку':'В версии остаётся разрыв'}</p><h3>\${escapeHtml(data.title||'Результат')}</h3><p>\${escapeHtml(data.explanation||'')}</p>\${data.correct&&data.reveal?\`<p><strong>Что установлено:</strong> \${escapeHtml(data.reveal)}</p>\`:''}\`;box.scrollIntoView({behavior:'smooth',block:'center'})}catch(err){const box=$('[data-verdict]');box.hidden=false;box.className='aid-verdict is-wrong';box.innerHTML=\`<h3>Проверка временно недоступна</h3><p>\${escapeHtml(err.message||'Ваши допросы сохранены в этой вкладке.')}</p>\`;console.error(err)}finally{btn.disabled=false;btn.textContent='Проверить версию';saveState()}}`,
`function openTheory(){renderTheory();showView('theory')}\nfunction renderTheory(){const wrap=$('[data-theory-suspects]');wrap.innerHTML=suspects.map((s,i)=>\`<label class="aid-radio"><input type="radio" name="suspect" value="\${s.id}" \${i===0?'required':''}><span><strong>\${escapeHtml(s.name)}</strong> · \${escapeHtml(s.role)}</span></label>\`).join('');const form=$('[data-theory-form]');form.hidden=false;const box=$('[data-verdict]');box.hidden=true;if(state.verdict)renderVerdict(state.verdict)}\nfunction renderVerdict(data){const form=$('[data-theory-form]');const box=$('[data-verdict]');box.hidden=false;box.className=\`aid-verdict \${data.correct?'is-correct aid-case-closed':'is-wrong'}\`;if(data.correct){form.hidden=true;box.innerHTML=\`<p class="aid-kicker">Дело раскрыто</p><h3>Марина Лебедева призналась в похищении</h3><p>\${escapeHtml(data.explanation||'Доказательная цепочка замкнута.')}</p><div class="aid-resolution-grid"><div><small>21:31</small><strong>Персональный доступ</strong><span>E-14 и PIN открыли фонд.</span></div><div><small>21:34</small><strong>Ложное алиби</strong><span>Сетевой лог оставляет Марину внутри.</span></div><div><small>КАМЕРА</small><strong>Окно было известно</strong><span>Время перезапуска она выяснила заранее.</span></div><div><small>АЛИБИ</small><strong>Остальные исключены</strong><span>Антон и Лев подтверждены независимыми следами.</span></div></div>\${data.reveal?\`<p class="aid-resolution-reveal"><strong>Итог:</strong> \${escapeHtml(data.reveal)}</p>\`:''}<div class="aid-resolution-actions"><button class="aid-secondary" type="button" data-action="restart-case">Пройти дело заново</button><a class="aid-primary" href="../">Другие дела Mystery Logic</a></div>\`;box.querySelector('[data-action="restart-case"]')?.addEventListener('click',restartCase);const back=$('[data-action="back"]');if(back)back.textContent='← Посмотреть материалы дела'}else{form.hidden=false;box.innerHTML=\`<p class="aid-kicker">В версии остаётся разрыв</p><h3>\${escapeHtml(data.title||'Результат')}</h3><p>\${escapeHtml(data.explanation||'')}</p>\`}box.scrollIntoView({behavior:'smooth',block:'center'})}\nfunction restartCase(){sessionStorage.removeItem(STORAGE_KEY);sessionStorage.removeItem('ml_ai_demo_session');location.reload()}\nasync function submitTheory(ev){ev.preventDefault();if(state.busy)return;const form=ev.currentTarget;const fd=new FormData(form);const picked=fd.get('suspect');const reason=fd.get('reason')?.toString().trim()||'';if(!picked||reason.length<8)return;const btn=form.querySelector('button[type="submit"]');btn.disabled=true;btn.textContent='Проверяем цепочку…';try{const data=await api({action:'check_theory',suspect_id:picked,reason,discovered_note_ids:[...state.notes.keys()].filter(id=>!id.startsWith('SYS-')),discovered_evidence_ids:[...state.evidenceIds],question_counts:state.questionCounts});state.verdict=data;renderVerdict(data)}catch(err){const box=$('[data-verdict]');box.hidden=false;box.className='aid-verdict is-wrong';box.innerHTML=\`<h3>Проверка временно недоступна</h3><p>\${escapeHtml(err.message||'Ваши допросы сохранены в этой вкладке.')}</p>\`;console.error(err)}finally{btn.disabled=false;btn.textContent='Проверить версию';saveState()}}`,
'final verdict flow');
js=replaceOnce(js,
`$('[data-action="theory"]').addEventListener('click',openTheory);$('[data-action="back"]').addEventListener('click',()=>showView('workspace'));`,
`$('[data-action="theory"]').addEventListener('click',openTheory);$('[data-action="theory-inline"]').addEventListener('click',openTheory);$('[data-action="back"]').addEventListener('click',()=>showView('workspace'));`,
'inline theory listener');
fs.writeFileSync(jsPath,js);

let css=fs.readFileSync(cssPath,'utf8');
const finaleCss=`\n.aid-case-progress{margin:0 14px 14px;padding:16px 18px;display:flex;align-items:center;justify-content:space-between;gap:18px;border:1px solid rgba(143,196,170,.28);border-radius:14px;background:linear-gradient(135deg,rgba(143,196,170,.09),rgba(210,174,115,.045))}.aid-case-progress>div{min-width:0}.aid-case-progress .aid-kicker{margin-bottom:6px;color:var(--aid-ok)}.aid-case-progress strong,.aid-case-progress span{display:block}.aid-case-progress strong{font:500 20px/1.25 Georgia,serif}.aid-case-progress span{margin-top:5px;color:var(--aid-muted);font-size:12px;line-height:1.5}.aid-case-progress .aid-primary{flex:0 0 auto}.aid-composer.is-closed{opacity:.5}.aid-theory-hint{margin:0 0 12px;color:var(--aid-muted);font-size:11px;line-height:1.5}.aid-theory-box.is-ready{margin-top:auto;padding:18px;border:1px solid rgba(143,196,170,.25);border-radius:13px;background:rgba(143,196,170,.055)}.aid-theory-box.is-ready .aid-kicker{color:var(--aid-ok)}.aid-theory-box.is-ready .aid-secondary{border-color:rgba(210,174,115,.5);background:linear-gradient(135deg,rgba(224,192,141,.16),rgba(184,134,77,.11));color:var(--aid-gold-2)}.aid-case-closed{padding:34px}.aid-case-closed .aid-kicker{color:var(--aid-ok)}.aid-case-closed h3{font-size:clamp(31px,4vw,48px);max-width:800px}.aid-resolution-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin:24px 0}.aid-resolution-grid>div{padding:15px;border:1px solid var(--aid-line);border-radius:12px;background:rgba(255,255,255,.018)}.aid-resolution-grid small,.aid-resolution-grid strong,.aid-resolution-grid span{display:block}.aid-resolution-grid small{color:var(--aid-gold);font-size:9px;letter-spacing:.13em;text-transform:uppercase}.aid-resolution-grid strong{margin-top:5px;font-size:13px}.aid-resolution-grid span{margin-top:6px;color:var(--aid-muted);font-size:11px;line-height:1.45}.aid-resolution-reveal{padding-top:18px;border-top:1px solid var(--aid-line)}.aid-resolution-actions{display:flex;gap:12px;align-items:center;margin-top:24px}.aid-resolution-actions .aid-secondary{width:auto;min-width:180px}.aid-resolution-actions .aid-primary{text-decoration:none;text-align:center}@media(max-width:800px){.aid-case-progress{margin:0 10px 10px;display:block}.aid-case-progress .aid-primary{width:100%;margin-top:13px}.aid-resolution-grid{grid-template-columns:1fr 1fr}.aid-resolution-actions{align-items:stretch;flex-direction:column}.aid-resolution-actions .aid-secondary{width:100%}}@media(max-width:520px){.aid-resolution-grid{grid-template-columns:1fr}}\n`;
if(css.includes('.aid-case-progress{')) throw new Error('finale CSS already present');
css+=finaleCss;
fs.writeFileSync(cssPath,css);

let test=fs.readFileSync(testPath,'utf8');
test=replaceOnce(test,
`assert.match(html,/ai-detective-vslice\\.js\\?v=0\\.3\\.3/,'30-turn client must have a fresh cache key');`,
`assert.match(html,/ai-detective-vslice\\.js\\?v=0\\.3\\.4/,'case-finale client must have a fresh cache key');\nassert.match(html,/data-case-progress hidden/,'earned confession needs an explicit next-step transition');\nassert.match(html,/data-action="theory-inline">Оформить обвинение/,'confession transition must give the player a clear next action');`,
'cache contract');
test=replaceOnce(test,
`assert.doesNotMatch(client,/Ответственная — Марина/i,'solution must remain server-side');`,
`assert.doesNotMatch(client,/Ответственная — Марина/i,'solution must remain server-side before the server verdict');\nassert.match(client,/function hasConfession\\(\\)/,'client must recognize the server-earned confession state');\nassert.match(client,/Признание получено · оформите обвинение/,'confession must visibly end interrogation and point to accusation');\nassert.match(client,/function renderVerdict/,'checked theory needs a dedicated resolution renderer');\nassert.match(client,/Дело раскрыто/,'correct theory must produce an explicit completion state');\nassert.match(client,/data-action="restart-case"/,'completed case must offer a clean replay path');`,
'client finale contract');
fs.writeFileSync(testPath,test);

console.log('patched AI detective confession-to-finale flow');
