import fs from 'node:fs';
import path from 'node:path';
import {ensureDir,escapeHtml,estimate} from './common.mjs';
export function writeCatalog(siteRoot,cases,freeMeta,editorial){const groups=[...new Map(cases.map(x=>[x.set.id,x.set])).values()].sort((a,b)=>a.order-b.order||a.title.localeCompare(b.title,'ru'));const cards=(set)=>cases.filter(x=>x.set.id===set.id).map(item=>{const locked=!editorial&&item.access==='premium';return`<article class="case-card ${locked?'is-locked':''}" data-access="${item.access}" data-search="${escapeHtml(`${item.title} ${item.category||''} ${item.logicType||''}`.toLowerCase())}"><div class="case-head"><span class="case-num">Дело № ${item.number}</span><span class="case-free">${locked?'Полный том':item.access==='free'?'Бесплатно':'Редакторский доступ'}</span></div><h3>${escapeHtml(item.title)}</h3><p>${locked?`Категория: ${escapeHtml(item.category||'Логика')}. Материалы скрыты до покупки.`:`Короткое расследование на тему «${escapeHtml(item.category||'Логика')}».`}</p><div class="case-meta"><span>${escapeHtml(item.difficulty||'Среднее')}</span><span>${escapeHtml(item.category||'Логика')}</span><span>≈ ${estimate(item.difficulty)} минут</span></div><a href="../${item.path}">${locked?'Подробнее':'Открыть дело'}</a></article>`}).join('');const sections=groups.map(set=>`<section class="case-set"><div class="set-heading"><div><p class="ml-kicker">${escapeHtml(set.id)}</p><h2>${escapeHtml(set.title)}</h2><p>${escapeHtml(set.description||'')}</p></div><span>${cases.filter(x=>x.set.id===set.id).length} дел</span></div><div class="case-grid">${cards(set)}</div></section>`).join('');const html=`<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#07111d"><meta name="description" content="100 детективных дел Кто врёт?"><link rel="stylesheet" href="../assets/mysterylogic.css"><link rel="stylesheet" href="../assets/full-catalog.css"><title>100 детективных дел — Кто врёт?</title></head><body><header class="ml-header ml-shell"><a class="ml-brand" href="../"><span class="ml-brand-mark">ML</span><span class="ml-brand-copy"><strong>Mystery Logic</strong><small>Interactive investigations</small></span></a><nav class="ml-nav"><a href="../kto-vret/">О серии</a><a class="ml-nav-cta" href="../${freeMeta[0].path}">Начать</a></nav></header><main class="ml-shell"><section class="catalog-hero"><p class="ml-kicker">Первый том · полная библиотека</p><h1>100 дел, где ответ нужно доказать</h1><p>15 расследований доступны бесплатно. Ещё 85 адаптированы для полного первого тома.</p></section><div class="catalog-bar"><strong>100 расследований · 15 бесплатно · 85 в полном томе</strong><span>Мобильная версия 0.14.0</span></div><div class="catalog-tools"><input id="case-search" type="search" placeholder="Поиск по названию и категории"><div class="catalog-filters"><button class="is-active" data-filter="all">Все 100</button><button data-filter="free">15 бесплатных</button><button data-filter="premium">85 полного тома</button></div></div>${sections}</main><script src="../assets/generated/cases-index.js?v=0.9.1"></script><script src="../assets/dossier-model.js?v=0.9.1"></script><script src="../assets/dossier-progress.js?v=0.9.1"></script><script src="../assets/dossier-achievements.js?v=0.9.1"></script><script src="../assets/full-catalog.js?v=0.9.1"></script></body></html>`;ensureDir(path.join(siteRoot,'dela'));fs.writeFileSync(path.join(siteRoot,'dela/index.html'),html);}
