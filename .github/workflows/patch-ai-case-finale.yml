name: Patch AI detective case finale

on:
  push:
    branches: [feature/ai-detective-case-finale]
    paths:
      - 'tools/patch-ai-case-finale.mjs'
      - '.github/workflows/patch-ai-case-finale.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/ai-detective-case-finale
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Patch confession-to-finale UX
        run: node tools/patch-ai-case-finale.mjs
      - name: Validate patched files
        run: |
          node --check assets/ai-detective-vslice.js
          node tests/ai-detective-vslice-contract.mjs
      - name: Commit patch
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add detektivnaya-igra-s-ii/index.html assets/ai-detective-vslice.js assets/ai-detective-vslice.css tests/ai-detective-vslice-contract.mjs
          git diff --cached --quiet && exit 0
          git commit -m 'Connect AI confession to case finale'
          git push origin HEAD:feature/ai-detective-case-finale
