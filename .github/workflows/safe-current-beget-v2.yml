name: Safe current Mystery Logic release to Beget v2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This legacy safe-current v2 deploy path is retired."
          echo "Build the reviewed Beget ZIP with production-beget.yml and deploy it manually."
