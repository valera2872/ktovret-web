name: Deprecated safe current v2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Safe-current v2 is retired. It intentionally removes cases 101-110 and disables Volume II."
          echo "Use the full Volume II launch workflow instead."
