name: Deploy Mystery Logic to Beget

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-only:
    runs-on: ubuntu-latest
    steps:
      - name: Automatic deployment disabled
        run: |
          echo "Automatic Beget deployment is disabled for the restored product architecture."
          echo "Use the validated artifact from production-beget.yml and upload it manually to Beget."
