name: Safe current Mystery Logic release to Beget

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This legacy 10/90 safe-current deploy path is retired."
          echo "Build the reviewed Beget ZIP with production-beget.yml and deploy it manually."
