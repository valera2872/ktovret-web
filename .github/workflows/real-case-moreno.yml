name: Real case Moreno vertical slice

on:
  pull_request:
    branches: [main]
    paths:
      - 'realnye-dela/pozharnaya-lestnica-1991/**'
      - 'assets/real-case-moreno-*'
      - 'tools/real-case-moreno-*.mjs'
      - '.github/workflows/real-case-moreno.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  slice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate JavaScript
        run: |
          node --check assets/real-case-moreno-slice.js
          node --check assets/real-case-moreno-polish.js
          node --check tools/real-case-moreno-smoke.mjs
          node --check tools/real-case-moreno-visual-audit.mjs
      - name: Validate deduction-first browser flow
        run: node tools/real-case-moreno-smoke.mjs
      - name: Capture premium visual audit
        run: node tools/real-case-moreno-visual-audit.mjs
      - name: Upload slice audit
        uses: actions/upload-artifact@v4
        with:
          name: real-case-moreno-slice-audit
          path: artifacts/real-case-moreno
          retention-days: 7
