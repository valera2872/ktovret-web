(()=>{'use strict';const cfg=window.KtoVretWeb||{},root=document.querySelector('[data-ktv-root]');if(!root||!cfg.case)return;const c=cfg.case,stage=Array.isArray(c.answerStages)?c.answerStages[0]:null,key=cfg.storageKey||`ktovret:web:v1:${c.id}`,base={selectedOptionId:'',attempts:0,hintsUsed:0,solved:false,firstAnswerCorrect:false,importantSourceIds:[],note:''};const parse=v=>{try{const x=JSON.parse(v);return x&&typeof x==='object'?x:{}}catch{return{}}};let state={...base,...parse(localStorage.getItem(key))},feedback='';const save=()=>localStorage.setItem(key,JSON.stringify(state));const esc=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');const initials=(n='')=>n.trim().split(/\s+/).filter(Boolean).slice(0,2).map(p=>p[0]).join('').toUpperCase();const correct=stage?.correctOptionIds?.[0]||'',options=stage?.options||[],hints=[c.explanation?.reasoningSteps?.[0]||'Отделите общедоступные факты от скрытых данных.',c.explanation?.reasoningSteps?.[1]||'Проверьте, какие детали выводятся из времени, движения и массы.',c.explanation?.reasoningSteps?.[2]||'Сравните момент сообщения со временем фактического вскрытия.'];const progress=()=>state.solved?100:state.selectedOptionId?78:(state.hintsUsed||state.note.trim())?58:34;const materials=()=>(c.characters||[]).filter(x=>!String(x.id).startsWith('__')).map(x=>{const imp=state.importantSourceIds.includes(x.id);return`<article class="ktv-statement ${imp?'is-important':''}"><div class="ktv-person"><span class="ktv-avatar">${esc(initials(x.name))}</span><span><strong>${esc(x.name)}</strong><small>${esc(x.role)}</small></span></div><p class="ktv-quote">«${esc(x.statement)}»</p><div class="ktv-inline-actions"><button class="ktv-small-button" type="button" data-action="important" data-source-id="${esc(x.id)}">${imp?'Убрать отметку':'Отметить важным'}</button></div></article>`}).join('');const optionButtons=()=>options.map(o=>{const sel=state.selectedOptionId===o.id;return`<button class="ktv-option ${sel?'is-selected':''}" type="button" data-action="select" data-option-id="${esc(o.id)}" aria-pressed="${sel}"><span class="ktv-radio"></span><span>${esc(o.label)}</span></button>`}).join('');const hintList=()=>hints.slice(0,state.hintsUsed).map((h,i)=>`<div class="ktv-hint"><strong>Подсказка ${i+1}.</strong> ${esc(h)}</div>`).join('');const result=()=>{const clean=state.firstAnswerCorrect&&state.hintsUsed===0,e=(c.explanation?.evidenceFragments||[]).map(x=>`<blockquote>«${esc(x.quote)}»</blockquote>`).join(''),steps=(c.explanation?.reasoningSteps||[]).map(x=>`<li>${esc(x)}</li>`).join('');return`<section class="ktv-panel ktv-result" id="ktv-result" tabindex="-1"><span class="ktv-result-badge">${clean?'Чистое раскрытие':'Дело раскрыто'}</span><h2>Источник знания разоблачён</h2><p class="ktv-result-lead">${esc(c.explanation?.shortReason||'')}</p><p>${esc(c.explanation?.fullReason||'')}</p><ol class="ktv-steps">${steps}</ol><div class="ktv-evidence">${e}</div><div class="ktv-actions"><button class="ktv-primary" type="button" data-action="share">Бросить вызов другу</button><button class="ktv-secondary" type="button" data-action="reset">Пройти заново</button></div></section>`};function render(){root.innerHTML=`<div class="ktv-app"><header class="ktv-topbar"><div class="ktv-brand"><span class="ktv-mark">⌕</span><div><p class="ktv-kicker">Детективная игра</p><p class="ktv-brand-title">Кто врёт?</p></div></div><span class="ktv-progress-label">${state.solved?'Раскрыто':'Расследование'}</span></header><div class="ktv-progress"><span style="--ktv-progress:${progress()}%"></span></div><section class="ktv-hero"><div class="ktv-meta"><span class="ktv-chip">${esc(c.difficulty)}</span><span class="ktv-chip">${esc(c.category)}</span><span class="ktv-chip">${esc(c.logicType)}</span></div><p class="ktv-eyebrow">Дело № 066</p><h1 class="ktv-title">${esc(c.title)}</h1><p class="ktv-subtitle">Не угадывайте. Отделите факты от невозможного знания и докажите противоречие.</p></section><section class="ktv-panel ktv-paper"><p class="ktv-eyebrow">Что известно точно</p><h2 class="ktv-section-title">Материалы происшествия</h2><div class="ktv-case-text">${esc(c.intro)}</div></section><div class="ktv-grid ktv-two-column"><section class="ktv-panel"><p class="ktv-eyebrow">${esc(c.materialsLabel||'Показания')}</p><h2 class="ktv-section-title">Запись сотрудника</h2><div class="ktv-materials">${materials()}</div></section><aside class="ktv-panel ktv-paper ktv-notebook"><p class="ktv-eyebrow">Рабочий инструмент</p><h2 class="ktv-section-title">Блокнот аналитика</h2><p class="ktv-section-copy">Запишите ход рассуждения. Заметка сохраняется только в этом браузере.</p><textarea data-action="note" placeholder="Какая деталь кажется лишней?">${esc(state.note)}</textarea></aside></div><section class="ktv-panel ${state.solved?'ktv-hidden':''}" id="ktv-answer"><p class="ktv-eyebrow">Ваша версия</p><h2 class="ktv-section-title">${esc(c.question)}</h2><p class="ktv-section-copy">${esc(stage?.instruction||stage?.prompt||'')}</p><div class="ktv-options">${optionButtons()}</div>${feedback?`<div class="ktv-feedback" role="status"><strong>Версия не сошлась.</strong> ${esc(feedback)}</div>`:''}<div class="ktv-hints">${hintList()}</div><div class="ktv-actions"><button class="ktv-primary" type="button" data-action="submit" ${state.selectedOptionId?'':'disabled'}>Проверить версию</button><button class="ktv-secondary" type="button" data-action="hint" ${state.hintsUsed>=hints.length?'disabled':''}>${state.hintsUsed?'Следующая подсказка':'Взять подсказку'}</button></div></section>${state.solved?result():''}<p class="ktv-footer-note">Прогресс хранится локально. Регистрация для первого расследования не нужна.</p></div>`}const go=s=>requestAnimationFrame(()=>{const e=root.querySelector(s);e?.scrollIntoView({behavior:'smooth',block:'start'});e?.focus?.({preventScroll:true})});async function share(){const clean=state.firstAnswerCorrect&&state.hintsUsed===0,text=`${clean?'Чистое раскрытие':'Дело раскрыто'}: «${c.title}». Сможете найти невозможную деталь без подсказки?`,url=cfg.permalink||location.href;try{if(navigator.share){await navigator.share({title:'Кто врёт?',text,url});return}await navigator.clipboard.writeText(`${text}\n${url}`);alert('Вызов и ссылка скопированы.')}catch(e){if(e?.name!=='AbortError')prompt('Скопируйте вызов:',`${text}\n${url}`)}}root.addEventListener('click',async ev=>{const b=ev.target.closest('[data-action]');if(!b)return;const a=b.dataset.action;if(a==='select'){state.selectedOptionId=b.dataset.optionId||'';feedback='';save();render()}else if(a==='important'){const id=b.dataset.sourceId||'';state.importantSourceIds=state.importantSourceIds.includes(id)?state.importantSourceIds.filter(x=>x!==id):[...state.importantSourceIds,id];save();render()}else if(a==='hint'&&state.hintsUsed<hints.length){state.hintsUsed++;save();render();go('#ktv-answer')}else if(a==='submit'&&state.selectedOptionId){const ok=state.selectedOptionId===correct;state.attempts++;if(ok){state.solved=true;state.firstAnswerCorrect=state.attempts===1;feedback='';save();render();go('#ktv-result')}else{feedback=state.attempts>=2?'Проверьте, какая часть записи описывает содержимое, скрытое до физического вскрытия.':'Часть цепочки можно восстановить по движению стеллажа, времени и массе. Ищите данные другого типа.';save();render();go('#ktv-answer')}}else if(a==='share')await share();else if(a==='reset'){state={...base};feedback='';save();render();scrollTo({top:0,behavior:'smooth'})}});root.addEventListener('input',e=>{if(e.target.matches('[data-action="note"]')){state.note=e.target.value;save()}});render()})();