(()=>{'use strict';let filter='all';const input=document.querySelector('#case-search');if(!input)return;const apply=()=>{const q=(input.value||'').trim().toLowerCase();document.querySelectorAll('.case-card').forEach(card=>{const matchFilter=filter==='all'||card.dataset.access===filter;const matchText=!q||(card.dataset.search||'').includes(q);card.classList.toggle('is-hidden',!(matchFilter&&matchText));});document.querySelectorAll('.case-set').forEach(section=>section.classList.toggle('is-hidden',!section.querySelector('.case-card:not(.is-hidden)')));};document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{filter=button.dataset.filter;document.querySelectorAll('[data-filter]').forEach(item=>item.classList.toggle('is-active',item===button));apply();}));input.addEventListener('input',apply);})();
